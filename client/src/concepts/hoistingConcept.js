/**
 * JavaScript Concept: Hoisting
 * 
 * Demonstrates:
 * - Function Declaration Hoisting
 * - Variable Hoisting (`var`)
 * - Temporal Dead Zone (TDZ) (`let`, `const`, `class`)
 * - Function Expression vs Function Declaration
 * - Execution Context Creation vs Execution Phases
 */

export * from '../utils/hoisting';
import {
    evaluateReportWithHoisting,
    analyzeHoistingBehavior,
    simulateExecutionContextHoisting,
    calculateRescuePriority,
    getUrgencyLabel,
    estimateResponseTime,
    formatRescueSummary,
} from '../utils/hoisting';

export default {
    evaluateReportWithHoisting,
    analyzeHoistingBehavior,
    simulateExecutionContextHoisting,
    calculateRescuePriority,
    getUrgencyLabel,
    estimateResponseTime,
    formatRescueSummary,
};
